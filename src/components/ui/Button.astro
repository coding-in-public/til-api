---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"button"> {}

const { ...rest } = Astro.props;
---

<button class="bg-blue-500 text-white px-4 py-1.5 rounded-md" {...rest}>
  <slot />
</button>
